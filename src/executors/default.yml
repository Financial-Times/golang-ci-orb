parameters:
  golang-image-version:
    type: string
    default: "1"
docker:
  - image: golang:<<parameters.golang-image-version>>
    auth:
      username: $DOCKERHUB_USERNAME
      password: $COVERALLS_TOKEN
